{
  "kind": "collectionType",
  "collectionName": "publications",
  "info": {
    "name": "publication"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true
    },
    "autor": {
      "via": "publications",
      "plugin": "users-permissions",
      "model": "user"
    }
  }
}
